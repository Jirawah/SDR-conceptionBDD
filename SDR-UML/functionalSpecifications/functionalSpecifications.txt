Spécifications fonctionnelles - Non authentifié

    Page 1 – Écran d’accueil non connecté

        Contenu visible :

            - Logo / Slogan : "RDV Gaming Cheater Free" (cliquez sur le logo amène sur la page Accueil authentifié )
            - Titre : "Organise tes rendez-vous"
            - Slogan : "RDV Gaming Cheater Free"
            - Boutons ou liens visibles :
                • INSCRIPTION
                • CONNEXION
            - Informations promotionnelles :
                • Création d’équipes et de communautés éthiques
                • Connexion vocale simplifiée avec l’API Discord
                • Recherche de joueurs fairplay
                • Validation via l’API Steam


        Interactions utilisateur constatées :

            1.Accès à l’inscription (INSCRIPTION) :
                • L’utilisateur peut initier le processus d’inscription.
                • Renvoie très probablement vers un formulaire d’inscription (vu plus tard).

            2.Accès à la connexion (CONNEXION) :
                • Permet de s’identifier s’il dispose déjà d’un compte.

            3.Mode sombre (DARK MODE) :
                • Bascule possible de l’interface en dark/light mode.

            4.Accès indirect à des explications techniques :
                • L’utilisateur peut lire les avantages du système : sécurité, anti-cheat, Discord, Steam – ce qui prépare implicitement aux prérequis d’inscription.
                • Pas cliquables ici, mais préparent le terrain pour les conditions d’adhésion.





    Page 2 – Formulaire d'inscription

        Contenu visible :

            Champs de formulaire :
                • Pseudo (ex : “Utilisez celui de Discord”)
                • Email
                • Steam ID
                • Mot de passe
                • Répétez votre mot de passe

            Aide utilisateur :
                • Bouton ou lien “Comment obtenir ma Steam ID ?”
                    → déclenche un popup d'aide expliquant comment récupérer manuellement son Steam ID.

            Boutons cliquables :
                • CONNEXION DISCORD
                    → redirige l’utilisateur vers un salon vocal Discord spécifique.
                    Un bot Discord en attente sur le salon y détecte automatiquement la présence de l’utilisateur et transmet à l’application, via l'API officiel Discord (sans OAuth2):
                    → son ID, son discriminator, son pseudonyme, son avatar et son statut (en ligne / hors ligne).
                    → Le bot ignore les utilisateurs déjà traités.
                • INSCRIPTION (bouton de validation du formulaire)
                • Déjà inscrit ? CONNEXION (lien/bouton menant au formulaire de login)
                • Politique de confidentialité (lien vers une page dédiée aux informations de collecte et traitement des données)

            Mentions ou conditions affichées :
                • “Avoir un compte Steam”
                • “Avoir un compte Discord”

            Conditions obligatoires pour s'inscrire :
                • Le formulaire doit être entièrement complété
                • L'email ne doit pas déjà exister en BDD
                • Le Steam ID ne doit pas déjà exister en BDD
                • Le Steam ID doit être validé par l’API Web Steam (pas de ban de la part de Steam ou des développeurs des jeux de cette plateforme)
                    → La réponse attendue inclut : personaname, avatar, et les infos sur les bannissements :
                    (ex : banni VAC : Non, nombre de bans VAC, nombre de bans de jeu, jours depuis le dernier ban)
                    → la validation Steam est faite avant insertion en BDD, et que les infos récupérées (pseudo/avatar) peuvent être stockées ou affichées.

            Autre élément UX :
                • Bascule de thème sombre/clair disponible


        Interactions utilisateur constatées :

            1.Remplissage manuel du formulaire :
                • L’utilisateur saisit les champs Pseudo, Email, Steam ID, Mot de passe, et confirmation.
                • Le mot de passe est vérifié par comparaison des deux champs.
                • Tous les champs sont obligatoires pour activer le bouton d’inscription (le bouton "CONNEXION DISCORD" doit avoir été cliqué et les informations récoltées par le bot doivent avoir été renvoyé afin que l'utilisateur puisse cliquer sur "INSCRIPTION" pour finaliser celle-ci).

            2.Aide Steam ID :
                • Un clic sur “Comment obtenir ma Steam ID ?” ouvre un popup avec les explications nécessaires, sans changer de page.

            3.Connexion via Discord (vocale, sans OAuth2) :
                • Clic sur CONNEXION DISCORD → redirection vers un salon vocal Discord.
                • Le bot Discord détecte l’utilisateur connecté et envoie ses informations à l’application :
                ID + discriminator, pseudo, avatar, statut.
                • Le bot ignore les utilisateurs déjà identifiés précédemment.

            4.Vérifications côté serveur :
                • L’email est vérifié : s’il existe déjà → blocage.
                • Le Steam ID est vérifié :
                    - non présent en BDD,
                    - validé via l’API Web Steam (pseudo, avatar, statut de bannissements).

            5.Soumission de l’inscription (INSCRIPTION) :
                • L’utilisateur peut cliquer sur INSCRIPTION seulement si :
                    - le formulaire est complet,
                    - Discord est validé,
                    - l’email et le Steam ID sont valides.
                • Il est en suite redirigé vers la page CONNEXION.

            6.Accès à la connexion s'il a déjà un compte (Déjà inscrit ? CONNEXION) :
                • Lien qui redirige vers le formulaire de login.

            7.Consultation de la politique de confidentialité :
                •Lien vers une page décrivant en détail la collecte et l’utilisation des données.

            8.Thème sombre :
                • L’utilisateur peut activer ou désactiver le mode sombre à tout moment.
                • Le mode choisi reste actif sur les autres pages.





    Page 3 – Formulaire de connexion

        Contenu visible :

            Champs de formulaire :
                • Email
                • Mot de passe

            Boutons cliquables :
                • CONNEXION (bouton de validation du formulaire)
                • Mot de passe oublié ? (lien ou bouton redirigeant vers la page de procédure de récupération (Réinitialisation du mot de passe - Demande d'email))
                • Pas encore inscrit ? INSCRIPTION (renvoie sur la page d'inscription)

            Messages d’erreur potentiels (affichés dynamiquement) :
                • Email ou mot de passe incorrect
                • Compte inexistant ou désactivé
                • Champs vides ou mal remplis

            Autre élément UX :
                • Bascule de thème sombre/clair disponible


        Interactions utilisateur constatées :

            1.Saisie des identifiants :
                • L’utilisateur renseigne son email et son mot de passe.
                • Aucun des deux champs ne peut être vide pour activer le bouton CONNEXION.

            2.Soumission du formulaire de connexion (CONNEXION) :
                • En cliquant sur CONNEXION, les informations sont envoyées à l’application pour vérification.
                • Si les identifiants sont valides :
                    - L’utilisateur est redirigé vers la page d'accueil authentifié.
                • En cas d’erreur :
                    - Un message s’affiche (ex. : “Email ou mot de passe incorrect”).

            3.Réinitialisation du mot de passe (Mot de passe oublié ?) :
                • Si l’utilisateur clique sur ce lien :
                    - Il est redirigé vers une page dédiée à la récupération de mot de passe.

            5.Lien (Pas encore inscrit ? INSCRIPTION) :
                • Redirige l'utilisateur vers la page d'inscription.

            6.Thème sombre :
                • L’utilisateur peut activer ou désactiver le mode sombre à tout moment.
                • Le choix du thème reste actif sur les autres pages.





    Page 4 – Réinitialisation du mot de passe – Demande d’email

        Contenu visible :

            Champs de formulaire :
                • Email (champ de saisie unique)

            Textes d’information :
                • “Entrez l’adresse email associée à votre compte. Vous recevrez un lien pour réinitialiser votre mot de passe.”
                • Message de confirmation affiché après soumission :
                    → “Si un compte est associé à cette adresse, un email de réinitialisation a été envoyé.”

            Boutons cliquables :
                • ENVOYER (valide l’adresse email et déclenche l’envoi du lien de réinitialisation)
                • Retour (revient à la page de connexion)

            Messages d’erreur potentiels :
                • “Adresse email invalide”
                • “Veuillez entrer une adresse email”

            Autre élément UX :
                • Bascule de thème sombre/clair disponible


        Interactions utilisateur constatées :

            1.Saisie de l’adresse email :
                • L’utilisateur entre l’adresse email associée à son compte.
                • Le champ est obligatoire ; une validation empêche l’envoi si le champ est vide ou mal formé.

            2.Soumission du formulaire (ENVOYER) :
                • Si le champ est correctement rempli, un appel est effectué vers le backend.
                • Deux cas de réponse :
                    - Si un compte correspond : un email de réinitialisation est envoyé.
                    - Sinon : même message affiché, pour des raisons de sécurité (aucune indication sur l’existence ou non du compte).

            3.Retour à la connexion (Retour) :
                • L’utilisateur peut revenir à la page de connexion pour retenter un login.

            4.Thème sombre :
                • Comme sur les autres pages, l’interface permet à l’utilisateur de choisir entre clair et sombre.
                • La préférence est conservée pour la navigation future.





    Page 5 – Réinitialisation du mot de passe – Formulaire Nouveau MDP

        Contenu visible :

            Champs de formulaire :
                • Nouveau mot de passe
                • Confirmer le nouveau mot de passe

            Textes d’information :
                • “Veuillez choisir votre nouveau mot de passe pour votre compte.”
                • “Pour des raisons de sécurité, le mot de passe doit comporter au minimum 12 caractères.”

            Boutons cliquables :
                • RÉINITIALISER (valide le nouveau mot de passe et enregistre la modification, puis redirige vers la page CONNEXION)
                • Retour (vers la page de Réinitialisation du mot de passe - Demande d'email)

            Messages d’erreur potentiels :
                • “Les mots de passe ne correspondent pas”
                • “Mot de passe trop court”
                • “Lien expiré ou invalide”

            Messages de confirmation :
                • “Votre mot de passe a été mis à jour avec succès. Vous pouvez désormais vous connecter.”

            Autre élément UX :
                • Bascule de thème sombre/clair disponible


        Interactions utilisateur constatées :

            1.Saisie du nouveau mot de passe :
                • L’utilisateur saisit un nouveau mot de passe puis le confirme dans un second champ.
                • Le système vérifie que :
                    - les deux mots de passe correspondent,
                    - le mot de passe respecte les critères de sécurité (ex. : 12 caractères minimum).

            2.Soumission du formulaire (RÉINITIALISER) :
                • Si la validation est réussie, le mot de passe est mis à jour dans la base de données.
                • L’utilisateur est redirigé vers la page de connexion.

            3.Lien expiré ou invalide :
                • Si l’utilisateur a cliqué sur un lien de réinitialisation trop ancien ou déjà utilisé, un message d’erreur s’affiche et il est invité à refaire la procédure depuis le début.

            4.Retour à la page Réinitialisation du mot de passe - Demande d'email (Retour) :
                • L’utilisateur peut revenir à la page d'avant sans réinitialiser son mot de passe.

            5.Thème sombre :
                • L’utilisateur peut activer/désactiver le thème sombre.
                • La préférence est conservée.





Spécifications fonctionnelles - Authentifié--   

    Page 1 – Accueil authentifié

        Contenu visible :

            En-tête persistante (présente sur toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Heure affichée
                • Menu de navigation :
                    → MON PROFIL
                    → PLANNING
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS

            Zone latérale gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre de RDV de l'utilisateur
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Encadré central contextuel :
                • Message incitatif :
                    → “Créer tes RDV ou demande à participer aux autres membres”
                • Bouton call-to-action :
                    → C’EST PAR ICI ! (redirige vers la page PLANNING RDV)

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers leurs profils

            Autre élément UX :
                • Bouton de bascule DARK MODE toujours visible


        Interactions utilisateur constatées :

            1.Navigation via le menu principal :
                - L’utilisateur peut naviguer vers les différentes sections du site en un clic :
                    • MON PROFIL → page de profil joueur
                    • MES RDV → aperçu et gestion des RDV (créés, en attente, ou participations)
                    • MES AMIS → gestion des amis, listes, invitations
                    • RECHERCHE JOUEURS → recherche de joueurs et consultation de profils
                    • PLANNING → calendrier des RDV par jour

            2.DÉCONNEXION :
                - Déconnexion immédiate de l’utilisateur, retour vers la page d’accueil non connecté.

            3.Affichage des données personnelles :
                - Avatar, pseudo, date et horaire prochain horaire, nombre de RDV concerné par l'utilisateur.

            4.Appel à l’action principal :
                - Le bouton C’EST PAR ICI ! redirige vers la section “Planning” avec des carbox des RDV (1 cardbox = une date) à venir et options de création.

            5.Bascule de thème sombre :
                - L’utilisateur peut activer/désactiver le dark mode. L’état est conservé (via localStorage ou autre).

            6.Consultation des amis connectés :
                - Liste visible avec état en temps réel (si WebSocket ou polling mis en place).
                - Si l'utilisateur clique sur une cardbox représentant un de ses amis, il est redirigé vers le profil de cet ami.





    Page 2 – Planning RDV

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Heure affichée
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latérale gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Planning des RDV (agenda) :
                • Titre de section :
                    → “PLANNING RDV”
                • Sous-titre répétitif par cardbox :
                    → “LES RDV POUR LE [date sélectionnée]”
                    Chaque cardbox représente une date distincte (ex. : 2/12) et affiche ce sous-titre individuellement.
                • Contenu d’une cardbox (résumé d’une journée) :
                    - Nom du jour concerné (ex. : "2/12")
                    - Répartition des RDV par jeu :
                        → Indication du nombre de RDV créés pour chaque jeu (ex. : PUBG : 5 RDV, Rust : 2 RDV, Among Us : 1 RDV)
                    - Synthèse de la participation de l’utilisateur connecté pour cette date :
                        → Si l’utilisateur a créé un ou plusieurs RDV, l'information est mentionnée
                        → Si l’utilisateur est invité à un RDV ou plusieurs RDV (et n’a pas encore répondu), l’invitation est signalée
                        → Si l’utilisateur a accepté une ou plusieurs participations à un RDV, cela est également affiché
                    - Bouton "VOIR" :
                        → Permet de consulter les détails complets des RDV liés à cette date (redirige vers une page dédiée : Détails du RDV - Cardbox cliquée)

             Zone latérale droite :
                • Calendrier :
                    → Vue mensuelle, jours cliquables
                    → Navigation possible entre mois (ex. : Janvier, Février…)
                    → Jours sélectionnables (highlighted quand actif)
                    → Affichage dynamique selon jour sélectionné (Lorsqu’un utilisateur clique sur un jour dans le calendrier (zone latérale droite), la zone principale se met à jour dynamiquement pour afficher une cardbox correspondant à cette date.)
                    → Feedback visuel pour les RDV déjà existants (surlignage) dans le calendrier latérale droit (Dans le calendrier mensuel, les jours pour lesquels les RDV de l'utilsiateur sont planifiés sont visuellement mis en valeur par rapport aux autres RDV)
                • Filtres disponibles :
                    → Par date
                    → Par jeu (PUBG, Rust, Among Us)
                    → Par rôle (organisateur, participant, invitation reçu en attente de confirmation...)
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers leurs profils

            Autre élément UX :
                • Messages ou infobulles d’aide accessibles (ex. : “Cliquez pour voir les détails ou rejoindre le RDV”)
                • Bouton de bascule DARK MODE toujours visible


        Interactions utilisateur constatées :

            1.Navigation dans le calendrier :
                • L’utilisateur peut :
                    → Passer d’un mois à l’autre
                    → Cliquer sur un jour spécifique du calendrier pour afficher les RDV correspondants dans la zone principale

            2.Filtrage des RDV :
                • En combinant des filtres :
                    → Par date
                    → Par jeu
                    → Par rôle (organisateur/participant)
                • Les résultats sont automatiquement filtrés et reflétés dans la zone principale

            3.Affichage et lecture d’une cardbox (zone principale) :
                • L’utilisateur visualise pour chaque date sélectionnée :
                    → Le nombre total de RDV créés pour cette journée
                    → La répartition par jeux (PUBG, Rust, Among Us)
                    → Un résumé de sa propre implication (Une mention visuelle indique si l’utilisateur connecté a créé un RDV, a été invité ou participe déjà à un RDV pour cette date)
                • Aucun détail n’est affiché directement ici, uniquement une synthèse rapide

            4.Accès aux détails d’une journée :
                • En cliquant sur le bouton “VOIR” présent sur une cardbox :
                    → L’utilisateur est redirigé vers la page Détails du RDV – Cardbox cliquée
                    → Cette action n’est possible que via ce bouton
                    → Aucun autre clic dans la cardbox n’est interactif

            5.Mode sombre :
                • Toujours disponible et conservé d’une page à l’autre





    Page 3 – Détails du RDV – Cardbox cliquée

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latéral gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Détail des RDV pour la date sélectionnée :
                • Titre de section : ex. “DÉTAILS POUR LE 2/12” (le 2/12 affiché dépend de la cardbox cliqué dans la page précédente - Planning RDV)
                • Bouton : C'EST PAR ICI (Créer un RDV à cette date) (redirige vers la page de création de RDV – Détails du RDV - Cardbox cliquée)
                • Pour chaque RDV listé :
                    → Nom du RDV : ex. PUBGNow
                    → Jeu associé : PUBG / Rust / Among Us
                    → Organisateur du RDV (son psuedonyme est affiché)
                    → Heure prévue (ex. : 20 h 30)
                    → Statut du salon : Public / Privé
                    → Liste des invités (speudonymes)
                • Statut personnel de l’utilisateur connecté :
                    → Si l’utilisateur est l’organisateur → bouton VOIR/MODIFIER
                    → Si l’utilisateur est invité → boutons ACCEPTER et REFUSER
                    → Si l’utilisateur participe déjà → bouton VOIR/MODIFIER
                    → Si l’utilisateur n'organise pas, n'est pas invité et si le salon est public → bouton DEMANDEZ A PARTICIPER

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers leurs profils

            Autre élément UX :
                • Bouton de bascule DARK MODE toujours visible


        Interactions utilisateur constatées :

            1.Consultation de la liste des RDV pour une date :
                • L’utilisateur voit tous les RDV de la date sélectionnée (ex. : 2/12), qu’il soit concerné ou non
                • Il peut consulter pour chaque RDV :
                    → Jeu, organisateur, heure, invités, statut du salon

            2.1.Utilisateur invité (si l'utilisateur à déjà cliqué sur accepter à une invitation)
                • le bouton Voir/Modifier remplace les boutons Accepter | Refuser

            2.2.Réponse à une invitation (si l'utilisateur invité n'a pas encore donné de réponse) :
                • Si le RDV est privé et l’utilisateur est dans la liste des invités :
                    → Deux boutons disponibles : ACCEPTER ou REFUSER
                • Si l'utilisateur accepte l'invitation, les boutons Accepter | Refuser devient le bouton Voir/Modifier
                • Si l'utilisateur refuse l'invitation, le slot prévu pour cette utilisateur devient disponible et si le salon a été créé en public, l'utilisateur peut demander à participer. Si le salon a été créé en privé, l'utilisateur n'ayant pas créé le RDV ne peut donc plus interagir avec ce RDV (plus de bouton affiché)

            3.Modification / suppression d’un RDV (si organisateur) :
                • Si l’utilisateur est organisateur :
                    → Boutons visibles : MODIFIER / SUPPRIMER
                    → Action redirige sur la page Détail invités - Organisateur - [nom du RDV]

            4.Demandez à participer (si l'utilisateur n'est pas l'organisateur, n'est pas invité au RDV, si la partie est en status public et s'il reste des slots disponible) :
                • Si le RDV a des places libres :
                    → L’utilisateur peut cliquer sur Demandez à participer

            6.Affichage dynamique des statuts :
                • Les statuts des invités sont actualisés en temps réel ou à chaque rechargement (confirmé, en attente, refusé)

            7.Thème sombre :
                • Toujours disponible, l’utilisateur peut le basculer à tout moment





    Page 4 – Détails du RDV – Créer un RDV
        
        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latéral gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Formulaire de création de RDV :
                • Titre de section : “CRÉER UN RDV POUR LE [date sélectionnée]”
                • Champ : Nom du RDV
                    → Champ texte libre (ex. : “Soirée Battle Royale”)
                • Sélecteur : Heure du RDV
                    → Menu déroulant ou sélecteur horaire (ex. : 20h30)
                • Sélecteur : Jeu concerné
                    → Liste déroulante : PUBG / Rust / Among Us
                    → Le nombre de slots disponibles pour les invités est automatiquement défini selon le jeu choisi :
                        - PUBG : 4 joueurs max → 3 places disponibles pour les invités (l'organisateur compte pour 1)
                        - Rust : 8 joueurs max → 7 places disponibles
                        - Among Us : 16 joueurs max → 15 places disponibles
                    → Le changement de jeu réinitialise les slots si un jeu différent est sélectionné
                • Sélecteur : Statut du salon
                    → Radio boutons : Public / Privé
                • Zone de slots disponibles : Inviter des amis
                    → Recherche dynamique de pseudo (champ déroulant où m'utilisateur peut cliquer sur le speudonyme d'un ami pour l'inviter au RDV, il prend donc le slot disponible.)
                    → L'utilisateur a aussi la possibilité de choisir parmis une des ses listes d'amis, dans ce cas les slots seront rempli par les utilisateurs de la liste d'ami choisit.
                • Bouton final : CRÉER LE RDV
                    → Valide les informations saisies et redirige vers la page "Détails du RDV - Cardbox cliquée" de la date sélectionnée

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers le profil de l’ami sélectionné

            Autre élément UX :
                • Bouton de bascule DARK MODE toujours visible
                • Gestion des erreurs en temps réel :
                    → Affichage d’un message d’alerte si un champ obligatoire est vide
                    → Encadré rouge autour des champs invalides
                • Pré-remplissage du champ date sélectionnée (hérité depuis la page précédente)


        Interactions utilisateur constatées :

            1.Saisie des données du RDV :
                • L’utilisateur peut entrer un nom personnalisé, choisir une heure, un jeu et un statut (public/privé).
                    → Si le salon est privée, les utilisateurs non invités ne peuvent interagir avec le salon (comme demander à participer par exemple)
                    → Si le salon est public, les utilisateurs non invités peuvent demander à participer, à condition qu'il reste des slots diponibles.

            2.Sélection des amis invités :
                • Recherche dynamique d’un pseudo dans la section INVITEZ DES AMIS.
                • Où choix d'un liste d'amis déjà établie (rempli les slots disponibles avec les speudonyme des joueurs de la liste d'amis).

            3.Validation du formulaire :
                • Le bouton CRÉER devient actif seulement si tous les champs obligatoires sont remplis.
                • En cas de succès :
                    → Redirection vers la page “Détails du RDV – Cardbox cliquée ([date sélectionnée])”
                • En cas d’erreur :
                    → Message explicite affiché sous le champ fautif

            4.Navigation persistante et contexte utilisateur conservé :
                • L’utilisateur peut à tout moment accéder à une autre section via le menu sans perdre sa session.

            5.Thème sombre :
                • L’utilisateur peut basculer à tout moment, l’état du thème est conservé.





    Page 5 – Mon profil
        
        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latéral gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Fiche profil joueur :
                • Titre de section : “MON PROFIL”
                • Informations personnelles affichées :
                    → Pseudo Discord (pseudo + discrim (ex. : JoueurX#1234))
                    → Pseudo Steam
                    → Avatar récupéré via Steam et Discord
                    → Email
                    → Steam ID
                • Bloc de statistiques de jeu :
                    → Temps de jeu estimé sur (récupéré via l'API Web Steam):
                        - PUBG
                        - Rust
                        - Among Us
                    → Nombre de RDV créés
                    → Nombre de participations
                    → Invitations en attente (si > 0)
                • Bloc de sécurité / options :
                    → Bouton : Changer de mot de passe (redirige vers une page dédiée)

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers le profil de l’ami sélectionné

            Autre élément UX :
                • Bascule de thème DARK MODE toujours visible
                • Les boutons d’action s’accompagnent d’infobulles explicatives :
                    → “Changer de mot de passe”
                    → “Modifier mon adresse mail”
                • Les champs sensibles (Steam ID) sont grisés et non modifiables
                • Retour visuel sur les actions réussies (ex : “Mot de passe modifié avec succès”)


        Interactions utilisateur constatées :

            1.Consultation de ses données :
                • L’utilisateur peut voir ses données de compte (pseudos et avatar (Discord et Steam), Steam ID, adresse email)
                • Les champs sont affichés et à coté du champ mot de passe, il y a un bouton "Modifier".

            2.Accès à la modification du mot de passe :
                • En cliquant sur le bouton "Modifier" (mot de passe), redirection vers la page - Réinitialisation du mot de passe - Demande d'email

            3.Consultation des statistiques :
                • Les temps de jeu, participations et statistiques sont récupérés dynamiquement (Steam API, base interne)

            4.Consultation de ses invitations en attente :
                • Si des RDV nécessitent sa réponse, une mention est affichée dans la section statistiques

            5.Navigation :
                • L’utilisateur peut cliquer sur les cardbox d’amis pour accéder à leurs profils
                • Peut naviguer vers une autre section via le menu en haut à tout moment

            6.Thème sombre :
                • Activable à tout moment, avec conservation de l’état sélectionné





    Page 6 – Mes RDV

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latéral gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)
            
            Zone principale – Liste des RDV liés à l’utilisateur :
                •Titre de section : “MES RDV”
                •Onglets de filtre :
                    → Liste des RDV que j'organise
                    → Liste des RDV auxquels je participe
                    → Liste des invitations à des RDV en attente de ma confirmation
            Pour chaque RDV affiché :
                • Nom du RDV (ex. : “PUBGNow”)
                • Jeu associé (PUBG / Rust / Among Us)
                • Date + Heure
                • Statut du salon : Public / Privé
                • Rôle de l’utilisateur (définit par les onlets de filtre):
                    → Organisateur / Invité / Participant
                • Boutons contextuels selon rôle :
                    → VOIR/MODIFIER si organisateur
                    → ACCEPTER | REFUSER si invité
                    → VOIR/MODIFIER si déjà confirmé

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers le profil de l’ami sélectionné

            Autre élément UX :
                • Bascule de thème DARK MODE toujours visible


        Interactions utilisateur constatées :

            1.Filtrage des RDV :
                • L’utilisateur voit ses RDV par rôle dans trois conteneurs différents :
                    → Liste des RDV que j'organise
                    → Liste des RDV auxquels je participe
                    → Liste des invitations à des RDV en attente de ma confirmation

            2.Réponse à une invitation :
                • Si l’utilisateur est invité à un RDV (statut en attente) :
                    → Deux boutons : ACCEPTER ou REFUSER
                    → L’état change selon l’action effectuée :
                        - Si l'utilisateur accepte l'invitation, le rdv change de conteneur pour passer du conteneur "Liste des invitations à des RDV en attentes de ma confirmation" au conteneur "Liste des RDV auxquels je participe", les boutons "Accepter | Refuser" devient le bouton "Voir/Modifier"
                        - Si l'utilisateur refuse l'invitation, le slot prévu pour cette utilisateur devient disponible (backend) et si le salon a été créé en public, l'utilisateur peut demander à participer. Si le salon a été créé en privé, l'utilisateur n'ayant pas créé le RDV ne peut donc plus interagir avec ce RDV (plus de bouton affiché)

            3.Consultation / modification / suppression d’un RDV :
                • Si l’utilisateur est organisateur :
                    → Il peut cliquer sur "Voir/Modifier" et sera redirigé vers "Détail invités - Organisateur - [RDV sélectonné] (PUBGNow par exemple)"

            4.Consultation simple :
                • Si l’utilisateur est déjà participant :
                    → Il peut accéder aux détails via "VOIR/Modifier" et sera alors redirigé vers "Détail invités - Participant - [RDV sélectonné] (par exemple CrazyPUBG)"

            5.Indicateur temporel :
                • Les RDV les plus proches sont mis en évidence en haut du tableau

            6.Navigation et session persistante :
                • L’utilisateur peut accéder aux autres sections à tout moment via le menu

            7.Thème sombre :
                • Bascule toujours disponible, avec état conservé entre pages





    Page 7 – Détail invités – Organisateur – [Nom du RDV] (ex. : PUBGNow)

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latéral gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Tableau des invités pour le RDV sélectionné (que j'organise) :
                • Titre de section : “ MON RDV - [RDV sélectionné] - DÉTAILS ET INVITÉS”
                • Sous-titre : Nom du RDV : [RDV sélectionné]
                • Le tableau affiche :
                    → Jeu : PUBG dans l'exemple
                    → Date et heure : 01/02/2025 à 14h20 dans l'exemple
                    → Statut du salon : Privé (ex)
                    → Nombre total de slots (1 organisateur + 3 participants dans l'exemple PUBGNow)
                    → Nombre de places restantes (1 slot disponible dans l'exemple)
                    → Le pseudo de chaque invité à la place des slots et les slots disponible indique : "Invitez un ami", si l'utilisateur clique sur cette celulle, il voit le speudonyme de ses amis apparaitre et il peut cliquer sur un ami pour qu'il remplisse le slot disponible.
                    → Leur temps de jeu total sur le jeu sélectionné
                    → Le statut : Confirmé / En attente
                    → Un bouton Supprimer pour retirer un invité en attente    
            Boutons principaux en bas de section :
                • REVENIR À MES RDV
                • SUPPRIMER LE RDV  

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers le profil de l’ami sélectionné

            Autre élément UX :
                • Bascule de thème DARK MODE toujours visible          


        Interactions utilisateur constatées :

            1.Consultation du RDV :
                • L’organisateur peut voir le jeu, la date, l’heure, et le statut du salon.

            2.Visualisation des invités :
                • Chaque invité est listé avec pseudo, temps de jeu et statut (Confirmé / En attente)

            3.Suppression d’un invité :
                • Si un invité est en attente, un bouton Supprimer est visible et actionnable par l'oganisateur du RDV.

            4.Ajout d’un nouvel invité :
                • En cliquant sur “Invitez un ami”, l’organisateur peut sélectionner un ami dans une liste déroulante

            5.Navigation :
                • Bouton pour revenir à la page Mes RDV
                • Bouton pour supprimer le RDV

            6.Thème sombre :
                • Activable en haut à droite
                • État conservé (ex : localStorage)





    Page 8 – Détail invités – Participant – CrazyPUBG

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latéral gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Détail du RDV (vue participant) :
                • Titre de section : “MA PARTICIPATION AU RDV – [Nom du RDV] – DÉTAILS ET INVITÉS”
                • Le tableau affiche :
                    → Jeu : PUBG (dans l’exemple CrazyPUBG)
                    → Date et heure : 12/06 à 20h30 (exemple)
                    → Organisateur : pseudo visible
                    → Statut du salon : Public / Privé
                    → Nombre total de slots (1 organisateur + X participants)
                    → Nombre de places occupées / restantes (ex. : 3 / 4 joueurs)
                    → Partie concernant l'utilisateur connecté (participant) :
                        - Bouton Annuler : SE RETIRER DU RDV
                        - Permet au participant de libérer sa place dans le RDV
                • Tableau des invités :
                    → Chaque ligne affiche :
                        - Pseudo
                        - Temps de jeu sur le jeu concerné (si disponible)
                        - Statut : Confirmé / En attente                     
                        - Avatar joueur (si affiché)
                            → L’utilisateur ne peut ni modifier, ni inviter, seulement consulter ou annuler sa participation

            Zone latérale droite :
                • amis (les amis apparaissent les uns au dessous des autres via des cardbox dans ce conteneur)
                    → Statut des amis apparent (ONLINE/OFFLINE)
                    → Les cardbox des amis sont cliquables est redirigent vers le profil de l’ami sélectionné

            Autre élément UX :
                • Bascule de thème DARK MODE toujours visible  
                • Infobulle d’aide au survol du bouton “SE RETIRER DU RDV”


        Interactions utilisateur constatées :

            1. Consultation des informations du RDV :
                • Le participant voit tous les détails du RDV :
                    → Jeu, date, heure, organisateur, statut du salon

            2. Retrait du RDV :
                • En cliquant sur “SE RETIRER DU RDV” :
                    → L’utilisateur quitte la participation
                    → Un message de confirmation peut s’afficher
                    → Le slot devient libre pour un autre joueur
                    → L’état change côté organisateur

            3. Visualisation des autres invités :
                • Tous les participants sont visibles
                • Les statuts sont lisibles en temps réel ou à chaque rafraîchissement

            4. Navigation persistante :
                • Possibilité de changer de section via le menu sans perdre le contexte

            5. Thème sombre :
                • Basculable à tout moment, état conservé (ex. via localStorage)
       





    Page 9 – Mes amis

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Heure affichée
                • Menu de navigation :
                    → MON PROFIL
                    → PLANNING
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS

            Zone latérale gauche (dashboard résumé) :           
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV
                → Horaire

            Zone principale – Gestion des amis :            
                • Titre de section : “MES AMIS”
                • Champ de recherche de joueur (pseudo)
                • Résultats dynamiques s’affichant sous le champ (avec statut et bouton “Ajouter”)
                • Liste des amis existants (présentés sous forme de cardbox avec informations suivantes) :
                    → Pseudo
                    → Avatar
                    → Statut (ONLINE / OFFLINE)
                    → Bouton : Supprimer
                    → Bouton : Ajouter à une liste
                • Gestion des listes d’amis (encart ou onglet) :
                    → Liste des groupes personnalisés créés par l’utilisateur
                    → Pour chaque groupe :
                        • Nom de la liste
                        • Nombre d’amis dans la liste
                        • Bouton : Modifier la liste (ajouter/supprimer un ami, renommer, supprimer la liste)

            Zone latérale droite :
                • amis (affichage identique aux autres pages)
                    → Statut ONLINE / OFFLINE
                    → Redirection vers le profil en cliquant sur une cardbox

            Autre élément UX :
                • Bouton de bascule DARK MODE toujours visible
                • Affichage de confirmation pour chaque action (ajout / suppression / ajout à une liste)
                • Infobulle au survol des boutons (ex. : “Ajouter à une liste”, “Supprimer cet ami”)
                • Affichage d’un message si aucun ami trouvé ou aucune liste créée
                    → “Vous n’avez pas encore d’amis ajoutés”


        Interactions utilisateur constatées :

            1. Ajout d’un ami :
                • L’utilisateur tape un pseudo dans la barre de recherche
                • Les suggestions s’affichent dynamiquement
                • Cliquer sur “Ajouter” envoie une demande d’ami (ou ajoute directement si mutuel)

            2. Suppression d’un ami :
                • Bouton “Supprimer” visible sur chaque cardbox ami
                • Une confirmation est affichée avant validation de l’action

            3. Création et gestion de listes d’amis :
                • L’utilisateur peut créer une liste personnalisée avec un nom
                • Il peut ajouter ou retirer un ami d’une liste
                • Il peut modifier le nom d’une liste ou la supprimer
                • Une même personne peut appartenir à plusieurs listes

            4. Navigation rapide :
                • Cliquer sur une cardbox d’un ami redirige vers son profil
                • Le menu de navigation permet de changer de page sans perte de contexte

            5. Feedback visuel et messages :
                • Infobulles explicatives au survol
                • Message contextuel si aucune donnée affichable (amis ou listes vides)

            6. Mode sombre :
                • Accessible à tout moment, état conservé






    Page 10 – Profil ami

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → PLANNING
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS

            Zone latérale gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date du prochain RDV de l'utilisateur connecté)
                    → Horaire (heure du prochain RDV de l'utilisateur connecté)

            Zone principale – Affichage du profil d’un ami :
                • Pseudo de l’ami consulté (ex. : “RustySniper”)
                • Avatar de l’ami
                • Statut en ligne (ONLINE / OFFLINE)
                • Liste des RDV à venir de cet ami (sous forme de mini-cardbox) :
                 → Titre de chaque RDV (ex. : “PUBGNow”, “Rust4Fun”)
                 → Date et heure du RDV
                 → Jeu concerné
                 → Indication du rôle de l’ami : Organisateur / Participant
                 → Bouton : VOIR LE RDV (redirige vers la page Détails du RDV – Cardbox cliquée)
             • Boutons d’interaction avec cet ami :
                 → RETIRER DE MES AMIS (confirmation demandée avant suppression)
                 → AJOUTER À UNE LISTE (affiche un mini-menu déroulant avec les listes existantes de l’utilisateur connecté)

            Zone latérale droite :
                • amis (cardbox verticales des autres amis de l’utilisateur connecté)
                    → Statut des amis apparent (ONLINE / OFFLINE)
                    → Chaque cardbox est cliquable et redirige vers le profil de l’ami concerné

            Autre élément UX :
                • Bouton de bascule DARK MODE toujours visible
                • Hover sur mini-cardbox RDV :
                 → Info-bulle précisant s’il reste des places ou si le RDV est complet
                • Confirmation modale lors de la suppression d’un ami
                • Message de validation après ajout à une liste


        Interactions utilisateur constatées :

            1. Consultation d’un profil ami :
                • L’utilisateur accède à cette page en cliquant sur une cardbox d’ami depuis :
                    → Accueil
                    → Planning RDV (zone latérale droite)
                    → Liste d’amis (page Mes Amis)

            2. Parcourir les RDV d’un ami :
                • L’utilisateur peut voir les prochains RDV de cet ami
                • Il peut cliquer sur un RDV pour en voir les détails (redirection vers page Détails du RDV - Cardbox cliquée)

            3. Retirer un ami :
                • Un clic sur “RETIRER DE MES AMIS” déclenche une boîte de confirmation
                • En cas de confirmation, l’ami est supprimé de la liste personnelle

            4. Ajouter à une liste :
                • Clic sur “AJOUTER À UNE LISTE” ouvre un menu déroulant
                • L’utilisateur choisit une de ses listes existantes
                • Confirmation visuelle de l’ajout (message de succès)

            5. Navigation persistante :
                • L’utilisateur peut naviguer ailleurs à tout moment sans perdre son état de session

            6. Thème sombre :
                • Bouton accessible en permanence
                • État conservé (via localStorage ou autre)





    Page 11 – Recherche joueurs

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Heure affichée
                • Menu de navigation :
                    → MON PROFIL
                    → PLANNING
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS

            Zone latérale gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                 → Nombre total de RDV
                    → Prochain RDV
                 → Horaire de ce prochain RDV

            Zone principale – Outil de recherche :
                • Titre de section : “RECHERCHE JOUEURS”
                • Champ de recherche par pseudo (autocomplétion) :
                    → Suggestion dynamique de pseudos lors de la saisie
                    → Résultat affiché sous forme de card utilisateur
                • Filtres additionnels disponibles :
                 → Par jeu joué récemment (PUBG, Rust, Among Us)
                 → Par statut (En ligne / Hors ligne)
                 → Par type de relation : amis / non-amis
                • Affichage des résultats :
                    → Chaque résultat apparaît sous forme de card utilisateur contenant :
                        • Pseudo
                        • Avatar
                        • Jeux joués récemment (icônes ou texte)
                        • Statut : en ligne / hors ligne
                        • Bouton : VOIR PROFIL (redirige vers la Page 10 – Profil ami)
                        • Bouton : AJOUTER EN AMI (si non-ami)

            Zone latérale droite :
                • Liste des amis connectés (format cardbox)
                    → Statut visible : ONLINE / OFFLINE
                    → Cardbox cliquable : redirige vers leur profil

            Autre élément UX :
                • Bouton de bascule DARK MODE toujours visible
                • Affichage clair si aucun résultat ne correspond à la recherche
                • Chargement des résultats en temps réel (via debounce ou autre)
                • Icône de chargement pendant les appels API


        Interactions utilisateur constatées :

            1. Recherche par pseudo :
                • L’utilisateur peut taper un nom partiel ou complet
                • Une liste dynamique de résultats s’affiche pendant la saisie

            2. Utilisation des filtres :
                • Les filtres permettent de restreindre les résultats visibles
                • Les résultats sont mis à jour dynamiquement

            3. Consultation d’un profil :
             • Cliquer sur “VOIR PROFIL” redirige vers la page Profil ami (Page 10)
             • Le contexte de navigation est conservé

            4. Envoi de demande d’amis :
                • Si un utilisateur trouvé n’est pas encore ami :
                 → Le bouton “AJOUTER EN AMI” apparaît
                 → Cliquer envoie la demande
                 → Un message de confirmation est affiché

            5. Thème sombre :
                • Disponible et conservé






    Page 12 – Profil joueur

        En-tête persistante (commune à toutes les pages authentifiées) :
            • Logo / Slogan : RDV Gaming Cheater Free
                → Cliquer sur le logo ramène à la page d’accueil authentifié
            • Menu de navigation :
             → MON PROFIL
             → MES RDV
             → MES AMIS
             → RECHERCHE JOUEURS
             → PLANNING

        Zone latérale gauche (dashboard résumé utilisateur connecté) :
            • Texte de bienvenue : “Bienvenue”
            • Pseudo utilisateur connecté affiché (ex. : “ByteStorm404”)
            • Avatar affiché de l’utilisateur connecté
            • Bouton : DÉCONNEXION
            • Bloc synthétique d’information :
                → Nombre total de RDV
                → Prochain RDV (date)
                → Horaire (heure)

        Zone principale – Profil public du joueur consulté :
         • Titre de section : “PROFIL DE [pseudo du joueur]”
         • Informations générales visibles :
                → Avatar du joueur
                → Pseudo
                → Statut (en ligne / hors ligne)
                → Date d’inscription
                → Nombre total de RDV créés
                → Nombre total de participations à des RDV
                → Taux de présence (participations validées / invitations acceptées)
            • Historique synthétique des derniers RDV publics auxquels le joueur a participé :
                → Affichage sous forme de cardbox miniatures avec :
                    • Nom du RDV
                    • Date et heure
                    • Jeu concerné
                    • Rôle dans le RDV : organisateur / participant

        Zone latérale droite :
            • Bouton principal (en fonction du contexte de l’utilisateur connecté) :
                → Si l’utilisateur n’a pas cet ami : “AJOUTER EN AMI”
                → Si l’utilisateur a déjà cet ami : “RETIRER DES AMIS”
            • Bouton secondaire (présent dans tous les cas) :
                → “INVITER À UN RDV” (redirige vers la page de création de RDV avec pré-sélection de ce joueur dans la liste)

        Autres éléments UX :
         • Bouton de bascule DARK MODE toujours visible
         • Infobulles sur les actions possibles (ajouter, retirer, inviter)
         • Retour possible via un bouton “← Retour à la recherche”


        Interactions utilisateur constatées :

            1. Consultation d’un profil joueur :
                • L’utilisateur peut consulter les informations publiques du joueur sélectionné depuis la page de recherche

            2. Demande d’ajout en ami ou suppression d’ami :
                • Si l’utilisateur n’a pas encore ajouté ce joueur :
                    → Bouton “AJOUTER EN AMI”
                • Si ce joueur est déjà un ami :
                    → Bouton “RETIRER DES AMIS”
                • Une confirmation est demandée pour la suppression

        3. Inviter à un RDV :
            • Quel que soit le statut d’amitié, l’utilisateur peut cliquer sur “INVITER À UN RDV”
            • Cela redirige vers la page de création de RDV, avec la date du jour sélectionnée et ce joueur pré-ajouté à la liste des invités (si slots disponibles)

        4. Navigation fluide :
            • Retour à la page précédente (ex. : résultats de recherche) grâce au bouton de retour
            • Accès continu au menu de navigation principal

        5. Affichage dynamique :
            • Les informations comme le statut en ligne, le nombre de RDV créés ou les participations sont mises à jour à chaque consultation (si l’API backend le permet)

        6. Thème sombre :
            • Bascule possible à tout moment
            • État mémorisé






    Page 13 – Liste d’amis

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Heure affichée
                • Menu de navigation :
                 → MON PROFIL
                 → MES RDV
                 → MES AMIS
                 → RECHERCHE JOUEURS
                 → PLANNING

            Zone latérale gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV (date)
                    → Horaire (heure du prochain RDV)

            Zone principale – Gestion des listes d’amis :
                • Titre : “MES LISTES D’AMIS”
                • Bouton : Créer une nouvelle liste
                    → Ouvre un formulaire léger dans une modale ou dans une zone inférieure :
                        • Champ : Nom de la liste
                        • Sélecteur multi-amis (liste déroulante avec recherche dynamique par pseudo)
                        • Bouton : VALIDER
                • Pour chaque liste existante :
                    → Nom de la liste (ex. : “Team PUBG”, “Amis IRL”)
                    → Nombre de membres dans la liste
                    → Liste des membres visibles sous forme de cardbox :
                        • Avatar + Pseudo + Statut (ONLINE / OFFLINE)
                        • Bouton “Voir profil” (redirige vers la Page 10 – Profil ami)
                        • Bouton “Retirer” (retirer l’ami de cette liste uniquement)
                • Boutons d’action en bas de la liste :
                    → Renommer la liste
                    → Supprimer la liste
                • Si aucune liste n’a été créée :
                    → Message informatif :
                        “Vous n’avez pas encore créé de liste. Cliquez sur le bouton ci-dessus pour en créer une.”

            Zone latérale droite :
                • amis (en-dehors des listes) :
                    → Cardbox affichant tous les amis de l’utilisateur
                    → Statut ONLINE / OFFLINE
                    → Bouton “Ajouter à une liste” (ouvre une petite popup avec les listes existantes)
                    → Bouton “Voir profil” (redirige vers le profil ami)

            Autres éléments UX :
                • Bouton de bascule DARK MODE toujours visible
                • Icônes claires pour chaque action (supprimer, modifier, ajouter…)
                • Feedback utilisateur :
                    → Confirmation après suppression ou ajout
                    → Messages d’erreur si une liste sans nom est validée


        Interactions utilisateur constatées :

            1. Création de liste d’amis :
                • L’utilisateur peut cliquer sur “Créer une nouvelle liste” pour générer une nouvelle liste d’amis personnalisée
                • Il peut nommer la liste, sélectionner des amis via recherche dynamique et valider

            2. Ajout / retrait dans une liste :
                • Les amis affichés dans la zone latérale droite peuvent être ajoutés à une ou plusieurs listes via un bouton “Ajouter à une liste”
                • Les amis déjà présents dans une liste peuvent être retirés individuellement de cette liste sans être supprimés des autres

            3. Navigation vers le profil :
             • L’utilisateur peut cliquer sur le bouton “Voir profil” sur chaque cardbox pour être redirigé vers la Page 10 – Profil ami

            4. Renommer ou supprimer une liste :
             • Chaque liste propose une option de renommage ou de suppression
             • Supprimer une liste ne supprime pas les amis de l'utilisateur, uniquement le regroupement

            5. Affichage dynamique du statut :
             • Les statuts des amis (ONLINE / OFFLINE) sont mis à jour en temps réel ou à chaque rechargement de page

            6. Thème sombre :
                • L'utilisateur peut basculer le thème, et le choix est conservé sur toutes les pages





    Page 14 – Créer une liste d’amis

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page Accueil authentifié
                • Heure affichée
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latérale gauche (dashboard résumé) :
             • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
                • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV
                    → Horaire du prochain RDV

            Zone principale – Formulaire de création de liste :
                • Titre de section : “CRÉER UNE NOUVELLE LISTE D’AMIS”
                • Champ : Nom de la liste
                    → Champ texte libre obligatoire (ex. : “TryHard PUBG”, “Soirées du jeudi”)
                • Zone de sélection des amis :
                    → Champ de recherche dynamique : l’utilisateur peut saisir un pseudo pour filtrer ses amis
                    → Résultats sous forme de cardbox avec pseudo + bouton AJOUTER
                    → Zone récapitulative des amis ajoutés à la liste (avec bouton RETIRER)
                • Bouton final : CRÉER LA LISTE
                → Devient actif uniquement si un nom de liste est saisi et au moins 1 ami sélectionné

            Zone latérale droite :
                • Affichage des amis (cardbox empilées)
                    → Statut ONLINE / OFFLINE visible
                    → Redirection possible vers Profil Ami via clic sur une cardbox
                • Accès rapide aux listes existantes (sous forme de liens)

            Autres éléments UX :
               • Bouton de bascule DARK MODE visible en permanence
                • Gestion des erreurs en temps réel :
                    → Champ vide : encadré rouge + message type “Nom requis” ou “Ajoutez au moins un ami”
                • Validation visuelle des sélections :
                    → Les amis ajoutés s’affichent en haut ou dans une zone latérale dédiée


        Interactions utilisateur constatées :

            1. Saisie et validation du nom de la liste :
                • Le champ ne peut pas être vide
                • Le nom peut être modifié jusqu’à validation

            2. Sélection d’amis à inclure :
                • L’utilisateur peut chercher des amis via pseudo
                • Il peut ajouter plusieurs amis à la liste
                • Un ami déjà ajouté ne peut plus être sélectionné à nouveau
                • Il est possible de retirer un ami sélectionné avant de valider

            3. Création de la liste :
                • Si le nom et au moins 1 ami sont renseignés, clic sur “CRÉER LA LISTE” enregistre la liste
               • Redirection automatique vers la page “Liste d’amis” après confirmation

            4. Thème sombre :
                • Commutateur disponible à tout moment
               • L’état du thème est mémorisé (via localStorage ou autre)





    Page 15 – Détail d’une liste d’amis

        Contenu visible :

            En-tête persistante (commune à toutes les pages authentifiées) :
                • Logo / Slogan : RDV Gaming Cheater Free
                    → Cliquer sur le logo ramène à la page d’accueil authentifié
                • Menu de navigation :
                    → MON PROFIL
                    → MES RDV
                    → MES AMIS
                    → RECHERCHE JOUEURS
                    → PLANNING

            Zone latérale gauche (dashboard résumé) :
                • Texte de bienvenue : “Bienvenue”
                • Pseudo utilisateur affiché (ex. : “ByteStorm404”)
                • Avatar affiché de l’utilisateur connecté
             • Bouton : DÉCONNEXION
                • Bloc synthétique d’information :
                    → Nombre total de RDV
                    → Prochain RDV
                    → Horaire du prochain RDV

            Zone principale – Contenu de la liste d’amis sélectionnée :
                • Titre de section : “Liste : [nom de la liste sélectionnée]”
                • Nombre total d’amis dans cette liste (ex. : “4 amis dans cette liste”)
                • Liste des amis affichée sous forme de cardbox :
                    → Pseudo
                    → Avatar
                    → Statut : ONLINE / OFFLINE
                    → Bouton : “Supprimer de la liste” (action immédiate)
                • Boutons d’action :
                    → “RENOMMER LA LISTE”
                        → Ouvre une modal ou un champ éditable pour modifier le nom
                    → “SUPPRIMER LA LISTE”
                        → Ouvre une fenêtre de confirmation avant suppression

            Zone latérale droite :
                • amis (liste classique en cardbox)
                    → Statut visible : ONLINE / OFFLINE
                    → Redirection vers leur profil joueur si cliqué

            Autre élément UX :
                • Bouton DARK MODE toujours visible
                • Affichage dynamique :
                    → Si un ami est supprimé → la liste se met à jour automatiquement
                • Message de confirmation ou d’erreur après chaque action (supprimer, renommer)


        Interactions utilisateur constatées :

            1. Consultation d’une liste d’amis :
                • L’utilisateur voit tous les amis associés à la liste sélectionnée

            2. Suppression d’un ami de la liste :   
                • Bouton “Supprimer de la liste” → Retire immédiatement l’ami
                • Feedback visuel : cardbox supprimée + message “Ami retiré”

            3. Renommage de la liste :  
                • En cliquant sur “RENOMMER LA LISTE” → champ d’édition ou modal
                • Validation déclenche la mise à jour du nom

            4. Suppression complète de la liste :   
                • Clic sur “SUPPRIMER LA LISTE” → pop-up de confirmation
                • En cas de validation → redirection vers la page “Liste d’amis” (Page 13)

            5. Navigation :
                • En-tête toujours accessible pour changer de page

            6. Dark mode :
                • Disponible à tout moment





    Page 16 – Notifications

        Toutes les notifications :
            • Invitations à des RDV
            • Mises à jour d’amis
            • Réponses à des demandes

            • Mise en gras ou surbrillance des nouvelles notifications
            • Info-bulles sur les icônes pour expliquer leur signification

        Texte de notification :
            • “Vous avez été invité au RDV ‘CrazyPUBG’ – cliquez ici pour voir.”
            • “L’utilisateur RedSky42 vous a ajouté à sa liste d’amis.”
            • “Votre demande de participation au RDV ‘RustRaid’ a été acceptée.”
            • Date et heure de la notification
            • État : non lue (mise en valeur) ou lue (affichage normal)
            • Bouton / Lien contextuel (ex. : VOIR, ACCEPTER, REFUSER, ALLER AU PROFIL)


        Interactions utilisateur constatées :

            1.Consultation des notifications :
                • À chaque connexion, les nouvelles notifications sont visibles automatiquement en haut
                • L'utilisateur peut cliquer sur une notification pour accéder à l'action ou page liée

            2.Gestion des notifications :
                • Clic sur une notification la marque comme lue
                • Possibilité d’archiver ou supprimer une notification (option à droite de chaque cardbox)

            3.Navigation par filtre :
                • L’utilisateur peut trier les notifications par type pour faciliter la lecture
                • Le filtre reste actif tant qu’il n’est pas désélectionné

            4.Thème sombre :
                • Comme sur toutes les pages, peut être activé/désactivé, état conservé